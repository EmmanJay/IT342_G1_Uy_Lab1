spring.application.name=uy

# MySQL Database Configuration (XAMPP / phpMyAdmin)
spring.datasource.url=jdbc:mysql://localhost:3306/it342_uy_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Secret Key
jwt.secret=IT342UyLab1SecretKeyForJWTAuthentication2026SuperSecure
jwt.expiration=86400000

# Server port
server.port=8080
